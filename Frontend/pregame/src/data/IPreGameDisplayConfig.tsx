export default interface IPreGameDisplayConfig {
    ScoreEnabled: boolean;
    SpellsEnabled: boolean;
    CoachesEnabled: boolean;
    ChampionStatsEnabled: boolean;
    LoadingScreenEnabled: boolean;
    Patch: string;
}